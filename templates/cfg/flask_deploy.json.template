{
  "schema": "merit.flask_deploy.v1",
  "project": "{{PROJECT_ID}}",
  "vercel_team": "agentdraven",
  "vercel_scope": "agentdraven",
  "vercel_org_id": "",
  "vercel_project_id": "",
  "production_url": "https://{{PROJECT_ID}}.vercel.app",
  "notes": "MERIT L1 §IX.E — deploy with .\\merit-live.ps1 deploy vercel --path <repo> (or vault merit.ps1 for operators). Never bare vercel --prod without --scope from this file."
}
