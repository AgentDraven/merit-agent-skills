---
name: merit-onboard
description: MERIT persona activate, repo onboard/verify, runtime out, foundation cert.
---

# merit-onboard

```powershell
.\scripts\merit.ps1 persona activate AgentDraven
.\scripts\merit.ps1 repo onboard <path>
.\scripts\merit.ps1 cert foundation <project-id>
```

Vault operators: `runtime out` · `runtime verify`.
