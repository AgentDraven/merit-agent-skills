---
name: merit-livealpha
description: >-
  Elevate a MERIT consumer toward live alpha — research floors, MeritSubs, dual dataplane.
---

# merit-livealpha

## Law

```powershell
.\merit.ps1 law --for-skill merit-livealpha
```

## OSS (always)

```powershell
.\merit.ps1 livealpha --path <consumer-repo>
.\merit.ps1 livealpha status --path <consumer-repo>
```

Research floors: ≥10 categories, ≥10 APA refs each, ≥50 cross-ref edges in `cfg/research_crossrefs.json`.

## Policy

Honesty bounds: no demoware claims for multi-user sync, email, or managed video without configured providers.

Vault L1 full policy: operators with plane **C** use affiliate runtime; OSS agents follow `merit.ps1 law LIVEALPHA`.

## Operator (plane C only)

Legal templates: vault `templates/legal/*` when publishing operator-grade legal pages.
