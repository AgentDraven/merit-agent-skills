# FR map — app_logic/

Fill from `docs/PRODUCT.prd.md` Must rows. Keep FR IDs stable.

| FR-ID | Module path | Acceptance check | Prove on cloud |
|-------|-------------|------------------|----------------|
| FR-01 | `app_logic/<slug>/` | (from PRD) | Open live app URL; … |

## Notes

- Should/Could stay out of this table until promoted to Must.
- Providers stay on merit-prod rails — modules compose them; they do not reimplement them.
